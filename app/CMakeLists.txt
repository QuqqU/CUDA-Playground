file(GLOB_RECURSE SRC_FILES CONFIGURE_DEPENDS
    ${CMAKE_CURRENT_SOURCE_DIR}/*.cc
)

add_executable(${PROJECT_EXECUTABLE} ${SRC_FILES})
target_link_libraries(${PROJECT_EXECUTABLE} PUBLIC ${PROJECT_LIBRARY})
